# API — Cloudflare Workers

> Coming in Phase C5

## Stack
- Cloudflare Workers
- D1 Database
- @website-analyzer/core

## Endpoints (planned)
- `POST /analyze` — Run full analysis
- `GET /history` — User scan history
- `GET /credits` — Check credits
